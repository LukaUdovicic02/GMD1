using System;
using UnityEngine;

namespace Game
{
    public class ExplosionDamageSystem : MonoBehaviour
    {
        /*private IDamagable takeDamage;
        private DestroyableObject destroyableObject;
        [SerializeField] private float radius;

        private void Update()
        {
            Collider2D[] coliiderArray = Physics2D.OverlapCircleAll(transform.position, radius);
            foreach (Collider2D collider in coliiderArray)
            {
                if (collider.gameObject.CompareTag("Player 1"))
                {
                    Debug.Log(destroyableObject.isExploded);
                    if (destroyableObject.isExploded)
                    {
                        Debug.Log("test 2");
                        takeDamage.TakeDamage(30);
                    }
                }
            }
        }*/
    }
}